---
title: MCP Server
description: Model Context Protocol server API reference for hosted OAuth and local stdio integrations.
keywords:
  - MCP server
  - Model Context Protocol
  - AI assistant integration
  - Claude
  - Cursor
canonicalUrl: https://lightfast.ai/docs/api-reference/sdks-tools/mcp-server
ogTitle: MCP Server API Reference – Lightfast
ogDescription: Model Context Protocol server API reference for hosted OAuth and local stdio integrations.
authors:
  - name: Lightfast Team
    url: https://lightfast.ai
    twitterHandle: "@lightfastai"
publishedAt: "2024-12-01T00:00:00Z"
updatedAt: "2026-06-01T00:00:00Z"
---

# MCP Server Reference

Lightfast exposes MCP tools through the hosted OAuth resource server and the `@lightfastai/mcp` local stdio package.

Hosted OAuth resource:

```text
https://mcp.lightfast.ai/mcp
```

Local development resource:

```text
https://[<wt>.]mcp.lightfast.localhost/mcp
```

The `@lightfastai/mcp` package remains available for API-key stdio clients.

## Installation

```bash
npx @lightfastai/mcp
# or
npm install -g @lightfastai/mcp
```

## CLI Options

```bash
npx @lightfastai/mcp [options]
```

| Option | Type | Default | Description |
|--------|------|---------|-------------|
| `--api-key` | `string` | `$LIGHTFAST_API_KEY` | Lightfast API key |
| `--base-url` | `string` | `https://lightfast.ai` | API base URL |
| `--help, -h` | - | - | Show help message |
| `--version, -v` | - | - | Show version |

## Environment Variables

| Variable | Description |
|----------|-------------|
| `LIGHTFAST_API_KEY` | API key (alternative to `--api-key` flag) |

---

## Tools

The MCP server exposes policy-derived tools to AI assistants.

### lightfast_system_health

Check whether the connection is authenticated and reachable.

### lightfast_signals_create

Create a signal in the connected organization.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `input` | `string` | Yes | Signal text |

### lightfast_signals_get

Get a visible signal by id from the connected organization.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | `string` | Yes | Signal id |

#### Example Request
```json
{
  "name": "lightfast_signals_create",
  "arguments": {
    "input": "Follow up with Ada about the launch review"
  }
}
```

---

## Error Responses

All tools return errors in the following format:

```json
{
  "error": "ERROR_CODE",
  "message": "Human-readable error message",
  "requestId": "req_abc123"
}
```

| Error Code | Description |
|------------|-------------|
| `UNAUTHORIZED` | Invalid or missing OAuth bearer token or API key |
| `VALIDATION_ERROR` | Invalid parameters |
| `NOT_FOUND` | Resource not found |
| `INTERNAL_ERROR` | Server error |

---

## Related

- [MCP Setup Guide](/docs/integrate/mcp) - Configuration for hosted OAuth and stdio clients
- [GitHub Repository](https://github.com/lightfastai/lightfast/tree/main/core/mcp) - MCP server source code
